pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                echo 'Hello World'
                sh 'docker-compose version'
                sh 'docker-compose up'
                // sh 'docker version'
            }
        }
    }
}
